# requirements.txt - WORKING VERSION FOR IMMEDIATE DEPLOYMENT

# Core Framework (FastAPI + Uvicorn)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (use psycopg3 instead of psycopg2 for better compatibility)
sqlalchemy==2.0.23
psycopg[binary]==3.1.18  # NEW: Use psycopg3 instead of psycopg2
redis==5.0.1

# Async HTTP
aiohttp==3.9.1
aiofiles==23.2.1

# Data Processing (use pandas 2.2.3 which has better Python 3.13 support)
pandas==2.2.3  # UPDATED: Newer version with better compatibility
numpy==1.26.4
orjson==3.9.12
pyyaml==6.0.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-multipart==0.0.6

# Web Scraping
beautifulsoup4==4.12.2
lxml==4.9.3
requests==2.31.0

# Utilities
python-dotenv==1.0.0
structlog==23.2.0

# Rate Limiting (skip limits package for now)
slowapi==0.1.8

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
