# ================================
# UG BOARD ENGINE - PRODUCTION REQUIREMENTS
# Optimized for Python 3.11+ (Ubuntu LTS 24.04 / GitHub Actions 2026)
# ================================

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation & Settings (2026 Standards)
pydantic==2.5.0
pydantic-settings==2.1.0

# TV Scraper Dependencies
aiohttp==3.9.0
beautifulsoup4==4.12.0
ffmpeg-python==0.2.0
requests==2.31.0

# Audio Fingerprinting Alternatives (Dejavu Replacement)
# ====================================================
# Since dejavu 2.1.0 doesn't exist, we use:
# 1. audfprint for local fingerprinting (lightweight, maintained)
# 2. librosa for audio processing (industry standard)
# 3. chromaprint for audio fingerprint generation

# Option A: Lightweight Fingerprinting Stack
audfprint==2020.6.22
librosa==0.10.2.post1
numpy==1.26.4
scipy==1.13.0

# Option B: Chromaprint (AcoustID) - Production Grade
# Requires: sudo apt-get install chromaprint-tools
pyacoustid==1.3.0

# Database (PostgreSQL + Redis for 2026 Architecture)
asyncpg==0.29.0
redis==5.0.0
aioredis==2.0.1

# Monitoring & Observability (2026 Standards)
prometheus-client==0.20.0
sentry-sdk==1.45.0
structlog==24.1.0

# Security (OWASP 2026)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.8

# Async Utilities
aiofiles==23.2.1
aiosqlite==0.20.0

# Development & Testing (CI/CD 2026)
pytest==8.1.1
pytest-asyncio==0.23.5
pytest-cov==4.1.0
black==24.4.2
mypy==1.9.0

# Optional: ACRCloud SDK (Commercial)
# pip install acrcloud
# Note: Requires commercial license
