# requirements.txt - Python 3.13 COMPATIBLE
# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (use versions that work with 3.13)
sqlalchemy==2.0.23
redis==5.0.1
# Skip asyncpg for now - build issues
# Skip psycopg2-binary for now - use psycopg3

# Async HTTP (use compatible versions)
aiohttp==3.9.1
aiofiles==23.2.1
aiosqlite==0.19.0

# Data Processing (use NO pandas or older version)
# pandas==2.1.4  # REMOVE - doesn't work with 3.13
numpy==1.26.4
orjson==3.9.10
pyyaml==6.0.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-multipart==0.0.6

# Audio Processing (check compatibility)
librosa==0.10.1  # May have issues
soundfile==0.12.1
# Skip numba for now - Cython issues

# Web Scraping
beautifulsoup4==4.12.2
lxml==4.9.3
requests==2.31.0

# Utilities
python-dotenv==1.0.0
structlog==23.2.0
prometheus-client==0.19.0
sentry-sdk==1.38.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2

# Rate Limiting (use compatible version)
slowapi==0.1.8
# Skip limits for now
