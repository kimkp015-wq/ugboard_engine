# EMERGENCY requirements.txt - Minimal working set
# Remove all broken dependencies and use latest stable versions

# Core Framework (FastAPI + Uvicorn)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
redis==5.0.1
psycopg2-binary==2.9.9

# Async HTTP
aiohttp==3.9.1
aiofiles==23.2.1
aiosqlite==0.19.0

# Data Processing
pandas==2.1.4
numpy==1.26.2
orjson==3.9.10
pyyaml==6.0.1

# Authentication (removed python-jose which has issues)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-multipart==0.0.6

# Audio Processing (your custom fingerprinter dependencies)
librosa==0.10.1
soundfile==0.12.1
numba==0.58.1
llvmlite==0.41.1

# REMOVED: aiohttp-client-cache (broken version)
# REMOVED: limits (broken version)
# REMOVED: chromaprint (doesn't exist)

# Web Scraping (for TV/Radio)
beautifulsoup4==4.12.2
lxml==4.9.3
requests==2.31.0

# Utilities
python-dotenv==1.0.0
structlog==23.2.0
prometheus-client==0.19.0
sentry-sdk==1.38.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2

# Rate Limiting ALTERNATIVE (remove limits, use slowapi standalone)
slowapi==0.1.8
